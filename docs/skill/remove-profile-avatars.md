# Remove Profile Avatars

Do not show generated profile/avatar initials in the nav user menu or workspace cards.

Keep username/menu behavior intact in `components/user-menu.tsx`, and keep workspace cards focused on workspace name, creator, stats, and progress in `modules/workspaces/components/workspace-card.tsx`.
