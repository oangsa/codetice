# Remove Profile Avatars

Do not show generated profile/avatar initials in the nav user menu or classroom cards.

Keep username/menu behavior intact in `components/user-menu.tsx`, and keep classroom cards focused on classroom name, creator, stats, and progress in `components/classrooms/classroom-card.tsx`.
