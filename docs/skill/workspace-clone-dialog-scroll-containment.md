# Workspace Clone Dialog Scroll Containment

The workspace clone dialog can include every question in a large workspace. Keep the modal shell fixed within the viewport and apply `overflow-y-auto overscroll-contain` only to the question-list container. This preserves the name field, explanatory copy, and clone action while users scroll and choose from long question lists.
