# User Menu Settings Entry

- Expose account maintenance actions from the username dropdown, not only through deep links.
- Put `Settings` above `Logout` so account maintenance is visible without crowding the menu.
- Keep the actual password update flow on `/settings` and use the dropdown as navigation, not as an inline form container.
