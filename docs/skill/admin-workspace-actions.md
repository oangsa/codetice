# Admin Workspace Actions

On the workspaces dashboard, admins should get the primary `Create Workspace` action.

Use `CreateWorkspaceForm` for admin users and keep `JoinWorkspaceForm` for non-admin users so the primary action matches the user's role.
