# Admin Classroom Actions

On the classrooms dashboard, admins should get the primary `Create Classroom` action.

Use `CreateClassroomForm` for admin users and keep `JoinClassroomForm` for non-admin users so the primary action matches the user's role.
