# Workspace Clone Publish-All Control

The workspace clone dialog should provide one master Publish all switch for large question sets. Enabling it includes and publishes every question that has at least one test case; questions without test cases remain cloned drafts because publication validation requires test coverage. Turning the master switch off clears publication while preserving individual inclusion choices.
