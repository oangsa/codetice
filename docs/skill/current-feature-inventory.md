# Current Feature Inventory

- When asked what the app currently has, summarize the implemented surface from actual routes, components, and services rather than from the original spec alone.
- Prefer grouping by user-facing capability: auth, student flows, admin tools, grading, and security.
- Put the inventory in `ignore/` when the user explicitly asks for a local reference file rather than product docs.
