# Passive Chip Hover

- Badges and chip-style status labels should not include hover color changes.
- Keep hover styling on real buttons and links, but neutralize it when a `Button` is visually used as a filter chip.
- For removable selected chips, keep focus styles on remove controls without hover-only color or background changes.
