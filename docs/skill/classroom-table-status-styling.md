# Classroom Table Status Styling

Classroom assignment tables should keep level and status cells visually compact.

Use soft, borderless level pills from `difficultyVariants`, and render status as a small colored dot plus text (`Todo`, `Failed`, `Passed`) instead of a badge. This keeps the classroom table closer to competitive-programming assignment tables and saves horizontal space.
