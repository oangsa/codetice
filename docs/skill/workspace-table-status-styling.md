# Workspace Table Status Styling

Workspace question tables should keep level and status cells visually compact.

Use soft, borderless level pills from `difficultyVariants`, and render status as a small colored dot plus text (`Todo`, `Failed`, `Passed`) instead of a badge. This keeps the workspace question list close to the main branch's competitive-programming layout and saves horizontal space.
