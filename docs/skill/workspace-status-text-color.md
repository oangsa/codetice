# Workspace Status Text Color

For workspace table statuses, keep the dot state-colored but render the text label in black.

Use `text-slate-950` for `Todo`, `Failed`, and `Passed` labels so status remains readable and the row does not become visually noisy.
