# ── Application ──────────────────────────────────────────────────────────────
DATABASE_URL=postgres://postgres:postgres@localhost:5432/vibe_grader
SESSION_SECRET=change-this-session-secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-admin-password

# ── Docker Compose (PostgreSQL service) ──────────────────────────────────────
POSTGRES_DB=vibe_grader
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-this-db-password

# ── Web server ────────────────────────────────────────────────────────────────
PORT=3000

# ── Grading worker ────────────────────────────────────────────────────────────
# Interval (ms) between job-polling cycles
GRADING_WORKER_POLL_MS=2000
# Max submissions processed per polling cycle
GRADING_WORKER_BATCH_SIZE=5
# Interval (ms) for worker-side Docker image inspect/pull checks
GRADING_IMAGE_PREP_INTERVAL_MS=60000

# ── Docker socket access (worker) ─────────────────────────────────────────────
# GID of the host docker group so the non-root worker can access the Docker
# socket. Find yours with: stat -c '%g' /var/run/docker.sock
DOCKER_GID=999
